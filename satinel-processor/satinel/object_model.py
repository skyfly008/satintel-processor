# Lightweight ML wrapper stub for object detection (SAM/YOLO)
from typing import List, Dict


def detect_objects(image, model: str = "sam") -> List[Dict]:
    # placeholder: return empty detections list
    return []
